# Immutable Archive

This directory contains the immutable v1.0 R1 publication ZIP, its SHA-256 sidecar, and the archive manifest. The ZIP intentionally excludes itself and its adjacent archive-control files.
